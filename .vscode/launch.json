{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Closkell Extension",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}/packages/vscode-closkell"
      ]
    }
  ]
}
