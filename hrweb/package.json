{
  "name": "closkell-hrweb",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check:closkell": "cargo run --manifest-path ../Cargo.toml -p cli -- check src/app.clsk",
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "npm run test:e2e",
    "test:e2e": "playwright test",
    "verify": "npm run check:closkell && npm run build && npm run test:e2e"
  },
  "dependencies": {},
  "devDependencies": {
    "@closkell/vite-plugin": "file:../packages/vite-plugin-closkell",
    "@tailwindcss/vite": "4.3.0",
    "@playwright/test": "^1.55.0",
    "tailwindcss": "4.3.0",
    "vite": "8.0.14"
  }
}
